# --- Web framework ---
Flask==2.3.2
gunicorn==23.0.0

# --- Database / ORM ---
SQLAlchemy==2.0.16
Flask-SQLAlchemy==3.0.5
psycopg2-binary>=2.9.9   # Postgres driver for Render

# --- Forms & validation ---
Flask-WTF==1.1.1
email-validator==2.0.0.post2

# --- HTTP & utilities ---
requests==2.31.0
python-dotenv==1.0.0
beautifulsoup4==4.12.2

# --- AI clients (pick according to your code) ---
# Keep openai==0.28 if you still call openai.ChatCompletion; otherwise upgrade to >=1.x and update your code.
openai==0.28
google-generativeai==0.8.5

# --- Build tools (fixes 'Cannot import setuptools.build_meta') ---
setuptools>=68
wheel>=0.42
